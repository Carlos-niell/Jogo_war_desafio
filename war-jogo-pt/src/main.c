#include <stdio.h>
#include "jogo/war.h"

int main() {
    // Inicializa o jogo
    inicializarJogo();

    // Configura o ambiente
    configurarAmbiente();

    // Inicia a partida
    iniciarPartida();

    return 0;
}